% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Prepare_scImmuAging.R
\name{.scImmuAgingMakePseudocells}
\alias{.scImmuAgingMakePseudocells}
\title{Generate pseudocells for downstream statistical analysis}
\usage{
.scImmuAgingMakePseudocells(
  expr,
  metadata,
  cellType,
  markerGenes,
  donorCol = "donor_id",
  ageCol = "age",
  cellTypeCol = "celltype",
  pseudocellSize = 15,
  pseudocellN = 100,
  replace = "dynamic",
  verbose = TRUE
)
}
\arguments{
\item{expr}{A numeric \code{matrix} of gene expression (rows: genes, cols: cells).}

\item{metadata}{A \code{data.frame} of cell-level metadata.}

\item{cellType}{A character string identifying the cell type to be processed.}

\item{markerGenes}{A character vector of genes to include in the pseudocell generation.}

\item{donorCol}{Column name in metadata for donor IDs. Defaults to \code{"donor_id"}.}

\item{ageCol}{Column name in metadata for subject age. Defaults to \code{"age"}.}

\item{cellTypeCol}{Column name in metadata for cell types. Defaults to \code{"celltype"}.}

\item{pseudocellSize}{Integer. The number of single cells to aggregate per pseudocell.}

\item{pseudocellN}{Integer. The target number of pseudocells to generate.}

\item{replace}{Logical or "dynamic". Whether to sample cells with replacement.}

\item{verbose}{Logical. Whether to print progress and missing gene messages.}
}
\value{
A \code{data.frame} of aggregated expression values (pseudocells),
with metadata columns (donorId, age) and gene expression columns.
}
\description{
This function creates "pseudocells" (also known as pseudo-bulk samples)
by aggregating single-cell expression data of a specific cell type.
It reduces technical noise and dropout effects by combining multiple
single cells into averaged expression profiles, which are more suitable
for linear modeling and differential aging analysis.
}
\details{
The function follows these steps:
\enumerate{
\item Filters expression data for the specific \code{cellType} and \code{markerGenes}.
\item Formats data using \code{.scImmuAgingPreprocessingCore}.
\item Groups data by donor and age.
\item Performs aggregation into pseudocells using the specified \code{pseudocellSize}.
}
}
