% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EPIC_GA.R
\name{epicGa}
\alias{epicGa}
\title{The Gestational Age (GA) clock based on 176 Illumina EPIC CpGs}
\usage{
epicGa(x, minCoverage = 0, verbose = TRUE)
}
\arguments{
\item{x}{A numeric matrix, \code{data.frame}, or \code{SummarizedExperiment}
object containing DNA methylation beta values. Rows should be CpG probes and
columns individual samples.}

\item{minCoverage}{A numeric value (0-1). The minimum proportion of
required CpGs that must be present. Default is 0.}

\item{verbose}{A logical flag. If \code{TRUE} (default), prints status messages.}
}
\value{
A named vector of predicted Gestational Ages (in weeks).
}
\description{
The Gestational Age (GA) clock based on 176 Illumina EPIC CpGs
}
\examples{
data(dnamExample)
beta_matrix <- dnamExample[[1]]

# Example 1: Direct Matrix Input
epicGaOut <- epicGa(x = beta_matrix, verbose = FALSE)
head(epicGaOut)

# Example 2: SummarizedExperiment Input
\dontrun{
  if (requireNamespace("SummarizedExperiment", quietly = TRUE)) {
    library(SummarizedExperiment)
    pheno_data <- dnamExample[[2]]
    rownames(pheno_data) <- colnames(beta_matrix)
    
    se_obj <- SummarizedExperiment(
      assays = list(beta = beta_matrix),
      colData = pheno_data
    )
    
    epicGaOut <- epicGa(x = se_obj, verbose = FALSE)
  }
}
}
\references{
Haftorn KL, Lee Y, Denault WRP, et al.
An EPIC predictor of gestational age and its application to newborns
conceived by assisted reproductive technologies.
\emph{Clin Epigenetics.} 2021
}
