% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/McCartney_Trait.R
\name{mcCartneyTrait}
\alias{mcCartneyTrait}
\title{Calculate Epigenetic Surrogates for Complex Traits (McCartney et al. 2018)}
\usage{
mcCartneyTrait(x, minCoverage = 0, verbose = TRUE)
}
\arguments{
\item{x}{A numeric matrix, \code{data.frame}, or \code{SummarizedExperiment}
object containing DNA methylation beta values. Rows should be CpG probes and
columns individual samples.}

\item{minCoverage}{A numeric value (0-1). The minimum proportion of
required CpGs that must be present. Default is 0.}

\item{verbose}{A logical flag. If \code{TRUE} (default), prints status messages.}
}
\value{
A \code{list} containing 10 named elements, one for each trait. Each element is
a \strong{named numeric vector} of the calculated epigenetic scores.
\itemize{
\item \strong{\code{BMI}}: Numeric vector of predicted scores.
\item \strong{\code{Smoking}}: Numeric vector of predicted scores.
\item \strong{\code{Alcohol}}: Numeric vector of predicted scores.
\item \strong{\code{Education}}: Numeric vector of predicted scores.
\item \strong{\code{Total_cholesterol}}: Numeric vector of predicted scores.
\item \strong{\code{HDL_cholesterol}}: Numeric vector of predicted scores.
\item \strong{\code{LDL_cholesterol}}: Numeric vector of predicted scores.
\item \strong{\code{Total_HDL_ratio}}: Numeric vector of predicted scores.
\item \strong{\code{WHR}}: Numeric vector of predicted scores.
\item \strong{\code{Body_fat_Perc}}: Numeric vector of predicted scores.
}
Each vector is named with the sample IDs from the \code{rownames} of \code{beta.m}.
}
\description{
Implements the elastic net (EN) predictors for 10 complex traits and
biomarkers using DNAm data, as described by McCartney et al. (2018).
}
\details{
This function computes epigenetic surrogate scores for 10 different traits
based on models trained on blood methylation data.

The function iterates through and calculates scores for the following traits:
\itemize{
\item \code{BMI} (Body Mass Index)
\item \code{Smoking} (Smoking pack-years)
\item \code{Alcohol} (Alcohol consumption units per week)
\item \code{Education} (Years of education)
\item \code{Total_cholesterol}
\item \code{HDL_cholesterol}
\item \code{LDL_cholesterol}
\item \code{Total_HDL_ratio} (Ratio of Total to HDL cholesterol)
\item \code{WHR} (Waist-to-Hip Ratio)
\item \code{Body_fat_Perc} (Body Fat Percentage)
}
}
\examples{
# Load the included example data (a list containing a matrix and phenotypes)
data(dnamExample)
beta_matrix <- dnamExample[[1]]

# ====================================================================
# Example 1: Direct Matrix Input
# ====================================================================
predRes <- mcCartneyTrait(x = beta_matrix)
head(predRes)

# ====================================================================
# Example 2: SummarizedExperiment Input
# ====================================================================
\dontrun{
  library(SummarizedExperiment)

  # Extract phenotype and ensure rownames match matrix colnames
  pheno_data <- dnamExample[[2]]
  rownames(pheno_data) <- colnames(beta_matrix)

  # Construct the SummarizedExperiment object
  se_obj <- SummarizedExperiment(
    assays = list(beta = beta_matrix),
    colData = pheno_data
  )

  # The function seamlessly accepts the Bioconductor object
  predRes <- mcCartneyTrait(x = se_obj)
  head(predRes)
}

}
\references{
McCartney DL, Hillary RF, Stevenson AJ, et al.
Epigenetic prediction of complex traits and death.
\emph{Genome Biol.} 2018
}
