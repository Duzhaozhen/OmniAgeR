% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Bernabeu_cAge.R
\name{bernabeuCAge}
\alias{bernabeuCAge}
\title{Calculate Chronological Age (cAge) using the hybrid model}
\usage{
bernabeuCAge(betaM, minCoverage = 0, verbose = TRUE)
}
\arguments{
\item{betaM}{A numeric matrix of DNA methylation beta values.
\code{rownames} (CpG probe IDs) and \code{colnames} (Sample IDs) are required.
The matrix should not contain \code{NA} values.}

\item{minCoverage}{A numeric value (0-1). The minimum proportion of
required CpGs that must be present. Default is 0.}

\item{verbose}{A logical flag. If \code{TRUE} (default), prints status messages.}
}
\value{
A named numeric vector of predicted ages. The names of the vector
correspond to the sample IDs (column names) from \code{betaM}.
}
\description{
This function loads pre-trained cAge model weights and applies
them to a user-provided methylation beta-value matrix.
It implements the hybrid model logic from the paper:
1. Predicts age using the standard linear model.
2. If the predicted age is < 20 years, it re-predicts using
the log(age) model.
It also automatically handles missing CpGs and NA values
via mean imputation.
}
\examples{
hannumBmiqM <- loadOmniAgeRdata(
    "omniager_hannum_example",
    verbose = FALSE
)[[1]]
bernabeuCAgeO <- bernabeuCAge(hannumBmiqM)
}
\references{
Bernabeu, E., McCartney, D.L., Gadd, D.A. et al.
Refining epigenetic prediction of chronological and biological age.
\emph{Genome Med.} 2023
}
